# Обновление: Админка + версия игры (beta)

Что добавлено:
- На стартовом экране появилась кнопка **Admin (панель)**.
- В шапке сайта отображается **версия игры** (по умолчанию: `0.1.3-beta`).
- Добавлены admin API и простой dashboard:
  - статистика сервера (uptime, комнаты, события, счётчики)
  - список комнат + просмотр деталей
  - «Песочница» (генерация тестовых данных, не трогает реальные комнаты)

## Важно (безопасность)
Админка защищена токеном `ADMIN_TOKEN`.

### Как включить
1) В Replit → Secrets/Environment Variables добавь:
- `ADMIN_TOKEN` = любой длинный токен (например 24+ символов)
- (опционально) `APP_VERSION` = например `0.1.3-beta`

2) Перезапусти сервер.

## Как пользоваться
1) Открой главную страницу игры.
2) Нажми **Admin (панель)**.
3) Введи `ADMIN_TOKEN` и нажми **Войти**.

## Технические детали
- Версия отдаётся по GET `/api/version`.
- Админ API:
  - GET `/api/admin/overview`
  - GET `/api/admin/room/:code`
  - POST `/api/admin/sandbox/generate`
  - POST `/api/admin/sandbox/reset`
Токен передаётся заголовком: `x-admin-token`.

## Sandbox v2 (Стенд для реальной комнаты)
Во вкладке **Sandbox** появилась секция **Стенд для реальной комнаты** — это быстрые админ‑действия для теста/отладки.
⚠️ Используй на тестовой комнате — эти действия меняют состояние выбранной комнаты.

Как пользоваться:
1) Создай комнату обычным способом (как ведущий).
2) В админке вставь **код комнаты** в поле.
3) Используй кнопки:
- **Добавить ботов** — добавляет игроков `Bot_*` в комнату.
- **Заполнить мемы** — подставляет тестовые картинки (SVG data URL) игрокам без мема.
- **Показать мемы** — включает `memesRevealed` (чтобы быстро увидеть мемы на экранах).
- **Начать vote** — форсирует фазу `vote`.
- **Авто-голоса** — раздаёт по одному голосу каждому online‑игроку (случайно, не за свой мем).
- **Сбросить раунд** — очищает мемы и сбрасывает флаги `hasMeme/hasVoted`, переводит в `collect`.

API (для справки):
- POST `/api/admin/sandbox/real/add-bots`
- POST `/api/admin/sandbox/real/fill-memes`
- POST `/api/admin/sandbox/real/reveal`
- POST `/api/admin/sandbox/real/force-vote`
- POST `/api/admin/sandbox/real/auto-vote`
- POST `/api/admin/sandbox/real/reset-round`


## Автотест (реальная игра, настоящие боты)
Во вкладке **Sandbox** добавлен **Автотест**:
- **🚀 Быстрый автотест** — проходит все этапы автоматически и формирует отчёт.
- **⏭ Пошаговый тест** — выполняет по одному этапу за нажатие (после каждого этапа мини‑отчёт).

Автотест использует настоящие Socket.IO подключения (host + несколько bot‑клиентов),
поэтому проверяет реальный игровой поток: создание комнаты → join → отправка мемов разных типов → голосование → финал.


## Ошибки (debug)
На вкладке **Обзор** карточка **Ошибок** теперь кликабельна: открывается окно с деталями последних ошибок и пояснением «по‑человечески».
